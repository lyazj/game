我刚刚做了一个极度简化版的揍揍包，设计了一套含有三个类的框架，目前靠Linux管道（所以暂时不能在Windows上跑）读写ASCII字符行进行IPC，模拟硬件通信（目前只需要三信号，非常简单）。

这个框架可以方便地改造成其它游戏（如果想做其它游戏的话）。

需要两个不同的终端同时运行./client和./server（应该可以直接使用我静态编译的版本）。
如果发现不正常，运行./compile.sh。

客户端先出牌（标准输入），例：
    client rock
服务器再出牌（标准输入），例：
    server paper
然后各自可以看到输出。还可以出scissor。
